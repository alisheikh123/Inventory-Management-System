@model IEnumerable<Inventory_Management_Systems.Models.tblVoucherDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TblVoucher.voucherCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Narration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.debitAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.creditAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TblVoucher.voucherCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Narration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.debitAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.creditAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.voucherdetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.voucherdetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.voucherdetailId })
        </td>
    </tr>
}

</table>
